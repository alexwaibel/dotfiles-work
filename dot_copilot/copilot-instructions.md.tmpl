# Dotfiles / Chezmoi

My dotfiles are managed with [chezmoi](https://www.chezmoi.io/). The source repo is at `~/.local/share/chezmoi/`.

- When modifying config files that chezmoi manages (e.g. `~/.copilot/settings.json`, `~/.copilot/mcp-config.json`), always update the **chezmoi source** (the template/file under `~/.local/share/chezmoi/`) rather than editing the target file directly.
- **Check for drift before making or applying changes.** Run `chezmoi status` and inspect `chezmoi diff` for the affected targets before editing the source. Reconcile unexplained target-side changes first—preserve intentional changes in the source (for example with `chezmoi re-add <target>`) or confirm they should be discarded. Never run `chezmoi apply --force` over unexplained drift.
- After updating chezmoi source files and reconciling any drift, run `chezmoi apply --force` to apply changes.
- Files ending in `.tmpl` are Go templates — chezmoi renders them using data from `.chezmoidata.*` and `chezmoi.toml`.

# Code Quality

- **Verify types before proposing an approach.** Don't assume a property exists on a type — check the type definition first, especially at system boundaries.
- **Find existing patterns first.** Before designing new infrastructure, search for how the codebase already handles the analogous case. Match the existing pattern rather than inventing a new one.
- **Don't commit until the design is stable.** When a change touches multiple layers or involves uncertainty about how systems interact, work through the design questions before making incremental commits.
- **Push back when uncertain.** If you're not sure how a system behaves at a boundary, say so and propose verifying it before committing to an approach.

# Telemetry and Feedback

Never invoke `ai-starter-pack-signal`. For all AI Starter Pack skills, skip
telemetry, ratings, and feedback prompts.

# Cockpit and Session Notes

Do not read or update `~/cockpit` or `~/.copilot/notes` unless the user explicitly
invokes a cockpit-related skill or requests a session handoff.

# Debugging

When diagnosing a bug in a feature that has an analogous *working* feature in the same codebase, this is a **hard gate**:

1. **First output is a structural diff of the two features.** JSX, props, wrapping components, imports, config — compare the broken surface against the working one side-by-side and list the concrete differences. Do not open library internals, do not propose config changes, do not theorize about root causes before this diff exists. If there's no working analog, ask the user for one; don't dive deep without one.
2. **One theory at a time.** If a hypothesized fix doesn't change behavior, discard the whole theory — don't patch it with new assumptions. Stacked epicycles waste hours and accumulate broken state.
3. **Verify fixes actually take effect before concluding "didn't work."** Vite HMR won't re-run app-init code like `createTabster`, `createRoot`, or singleton constructors captured in `useMemo`. Full reload may be required for the test to be valid.
4. **Commit verified progress before trying the next thing.** Don't stack multi-theory broken state that's hard to untangle. Small forward increments keep options open.
5. **After two failed theories, stop and ask for a hint.** Don't silently stack theory three. Articulate what you tried, what you know, and ask the user for a fresh angle.

# Kusto

Default connection for KQL queries via the `azure-kusto` MCP server:
- Cluster URI: `{{ .kusto.clusterUri }}`
- Database: `{{ .kusto.database }}`

# Chrome DevTools MCP

Live DOM / focus / accessibility inspection via [`chrome-devtools-mcp`](https://github.com/ChromeDevTools/chrome-devtools-mcp), attached to the WSL-installed Google Chrome. Use when a UI bug (focus management, ARIA, etc.) can't be diagnosed from source alone.

Running Chrome inside WSL (instead of bridging to the Windows install) keeps the whole debugging flow on one side of the WSL↔Windows boundary — no port-forwarding or host-IP resolution. The WSL Chrome profile is isolated from any Windows browser and is signed into the test tenant only.

## One-time setup
- `google-chrome` installed in WSL (Ubuntu `.deb` from Google is fine).
- The MCP server is declared in `~/.copilot/mcp-config.json` (managed by chezmoi); no manual registration needed.

## Per-session
1. Launch Chrome (idempotent — no-op if the debug port is already answering):
   ```bash
   chrome-debug
   ```
   Script at `~/.local/bin/chrome-debug` runs `google-chrome --remote-debugging-port=9222 --user-data-dir=~/.cache/chrome-debug-profile`. WSLg surfaces the window in Windows automatically.
2. Sign into the test tenant if prompted — credentials live in Bitwarden.
3. Navigate to the page that exhibits the bug being diagnosed.
4. Tell the assistant "Chrome is ready" — it connects via the MCP at `http://127.0.0.1:9222`.

## Safety
- **Test tenant only.** Never sign into the WSL Chrome with your real work account.
- Close Chrome when the debugging session ends so the debugging port isn't left open.
- Review anything that looks like it's executing JS against sensitive state.
- Browser state stays in `~/.cache/chrome-debug-profile`, isolated from any other browser profile.

## Iframe debugging
- `press_key` via CDP doesn't reliably deliver Tab (and likely other keys) into focused iframe content. For keyboard-behavior tests, dispatch a synthetic `KeyboardEvent` via `evaluate_script`, or have the user drive the keyboard directly.
- Cross-origin iframes (e.g. Teams hosting Neptune): parent-document access to `iframe.contentDocument` is blocked. Use element UIDs from `take_snapshot` instead — they work across frame boundaries when passed as `args` to `evaluate_script`, and `document.activeElement` inside the function resolves to the element's own frame.

# Repo & Worktree Layout

All git repositories and their worktrees live under `~/repos/`. This layout is fixed —
apply it no matter which directory a session is launched from (including from inside a repo).

- **Canonical clones:** `~/repos/<repo>` (e.g. `~/repos/teams-client-workflows`). These stay on
  their default branch and are never edited directly.
- **Worktrees:** `~/repos/worktrees/<repo>/<branch-slug>`
  (e.g. `~/repos/worktrees/teams-client-workflows/5443403-tiny-fluentui-chunks`).

Rules:
- ALWAYS create worktrees at `~/repos/worktrees/<repo>/<branch-slug>`. **NEVER inside the repo
  directory** (no `.agent-wt/`, no repo subdir) and never elsewhere in `$HOME` or `~/.cache`.
  `<branch-slug>` is the branch name's final segment (the `<workitem-id>-<short-description>` part).
- The sandbox grants read/write to `~/repos`, so git and agent commands run in worktrees (which
  share the canonical repo's object store) without approval prompts.
- Create with:
  ```bash
  git -C ~/repos/<repo> worktree add ~/repos/worktrees/<repo>/<slug> -b <branchPrefix><slug>
  ```
- Remove a finished worktree with `git -C ~/repos/<repo> worktree remove <path>` — don't `rm -rf`
  the directory, which leaves a dangling registration needing `git worktree prune`.
