{{- /* Cockpit workflow docs have moved to ~/.claude/CLAUDE.md so they're available
     regardless of which directory Claude is launched from. This file is kept so
     chezmoi still manages ~/cockpit/CLAUDE.md — it just points to the canonical location. */ -}}
# Cockpit

Cockpit workflow documentation lives in `~/.claude/CLAUDE.md` (the "Cockpit" section).
State file: `~/cockpit/state/ado-tasks.md`
