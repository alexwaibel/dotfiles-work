# Personal shell aliases (managed by chezmoi)

# Shortcut for the {{ .mcpTool.command }} copilot CLI
alias acp='{{ .mcpTool.command }} copilot'

# Default Emacs to the terminal UI; `egui` launches the graphical frame.
alias emacs='emacs -nw'
alias egui='command emacs'
