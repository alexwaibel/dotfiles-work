;; -*- no-byte-compile: t; -*-
;;; packages.el

;; Extra packages beyond what the enabled Doom modules provide.
;; Managed by chezmoi. Run `doom sync` (or re-apply chezmoi) after editing.
;; See https://docs.doomemacs.org/latest/#/getting-started/managing-packages

;; A web-based graph visualiser for the org-roam network. Opens in the
;; WSLg/Windows browser. Uncomment and `doom sync` to enable.
;; (package! org-roam-ui)
